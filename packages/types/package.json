{
  "name": "@creatordeck/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for types'",
    "build": "echo 'types are source-only'",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@creatordeck/config": "workspace:*",
    "typescript": "^5.4.5"
  }
}
